(define (domain wumpus2)
  (:requirements :strips)
  (:predicates
   (at ?what ?square)
   (adj ?square-1 ?square-2)
   (pit ?square)
   (wumpus-in ?square)
   (gold-in ?square)
   (have ?who ?what)
   (is-agent ?who)
   (dead ?who)
   (wasted ?what)
   (is-gold ?what))
  (:action move
    :parameters (?from ?to)
    :precondition (and (at agent ?from)
		       (adj ?from ?to)
		       (not (pit ?to))
		       (not (wumpus-in ?to)))
    :effect (and (not (at agent ?from))
		 (at agent ?to))
    )

  (:action grab
    :parameters (?where)
    :precondition (and(at agent  ?where)
		       (gold-in ?where))
    :effect (and (have agent the-gold)
		 (not (at the-gold ?where)))
    )
  (:action shoot
    :parameters (?where ?to)
    :precondition (and (is-agent agent)
		       (have agent the-arrow)
		       (at agent ?where)
		       (at wumpus ?to)
		       (adj ?where ?to))
    :effect (and
		 (not (wumpus-in ?to))
		 (not (have agent the-arrow))
		 (wasted the-arrow)
		 (dead wumpus))
    )
)